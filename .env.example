# Example environment variables for local development and production

# SMTP settings (used by api/send-contact.ts)
# For Gmail, create an App Password and use it as SMTP_PASS.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=kgos zqlk exsa cvej
SMTP_SECURE=false

# Optional: destination email for contact form
CONTACT_TO=norhene.chalghoumi@gmail.com

# NODE_ENV is optional
NODE_ENV=development

# NOTE: Do NOT commit real secrets. Copy this file to .env and fill credentials.
